# 🗺️ WasiCore 3D坐标系统指南

## 🚨 重要提醒 - AI开发助手必读

**WasiCore框架使用左手坐标系，与Unreal Engine保持一致，请务必理解以下关键信息：**

### 📐 3D坐标轴定义

```
WasiCore 3D坐标系统（左手坐标系）：
- X轴：水平方向（左右移动）
- Y轴：水平方向（前后移动，深度）  
- Z轴：高度方向（上下移动，跳跃方向）
- XY平面：地面/水平面
- Z轴正方向：向上

默认镜头设置：
- Yaw: -90°, Pitch: -70°, Roll: 0°
- 默认视角：俯视角2.5D镜头
- 默认镜头下的原点位置：左上角
```

### 🎯 关键约定

1. **地面定义**：XY平面（Z=0）是地面
2. **高度轴**：Z轴是高度轴，Z值越大越高
3. **跳跃方向**：角色跳跃时Z坐标增加
4. **重力方向**：重力沿Z轴负方向作用

### 🎮 实际应用示例

#### Vector3 坐标构建
```csharp
// 正确的3D位置创建
var groundPosition = new Vector3(100, 200, 0);    // 地面位置
var jumpingPlayer = new Vector3(100, 200, 50);    // 跳跃中的玩家（高50单位）
var flyingObject = new Vector3(100, 200, 150);    // 飞行物体（高150单位）
```

#### ScenePoint 使用
```csharp
// 地面上的位置
var groundPoint = new ScenePoint(new Vector3(x, y, 0), scene);

// 空中的位置（如跳跃、飞行）
var airPoint = new ScenePoint(new Vector3(x, y, height), scene);
```

#### 物理系统
```csharp
// 重力向下（Z轴负方向）
velocity.Z -= gravity * deltaTime;

// 跳跃向上（Z轴正方向）
velocity.Z = jumpForce;
```

#### 平台/地形创建
```csharp
// 地面平台（Z=0）
var groundPlatform = new ScenePoint(new Vector3(x, y, 0), scene);

// 高台（Z>0）
var elevatedPlatform = new ScenePoint(new Vector3(x, y, platformHeight), scene);
```

## ⚠️ 常见错误和避免方法

### 🚫 错误示例
```csharp
// ❌ 错误：将Y轴用作高度
var wrongJump = new Vector3(x, jumpHeight, z);

// ❌ 错误：重力作用在Y轴
velocity.Y -= gravity * deltaTime;
```

### ✅ 正确示例
```csharp
// ✅ 正确：将Z轴用作高度
var correctJump = new Vector3(x, y, jumpHeight);

// ✅ 正确：重力作用在Z轴
velocity.Z -= gravity * deltaTime;
```

## 🎲 游戏开发场景应用

### 1. 跳跃游戏
```csharp
// 玩家跳跃
playerVelocityZ = jumpForce;  // Z轴向上跳跃

// 重力下落
playerVelocityZ -= gravity * deltaTime;  // Z轴向下重力

// 平台碰撞检测
if (playerZ <= platformZ + platformHeight)
{
    // 玩家落在平台上
}
```

### 2. 飞行游戏
```csharp
// 飞机升降
aircraft.Position = new Vector3(x, y, altitude);  // Z为飞行高度

// 高度调整
altitude += climbRate * deltaTime;  // Z轴上升
```

### 3. 建筑系统
```csharp
// 多层建筑
var floor1 = new Vector3(x, y, 0);     // 一楼在地面
var floor2 = new Vector3(x, y, 300);   // 二楼高300单位
var floor3 = new Vector3(x, y, 600);   // 三楼高600单位
```

## 🔧 Shape系统中的坐标约定

### 形状原点位置
- **立方体/圆柱体**：底面中心（便于放置在地面）
- **球体**：几何中心
- **胶囊体**：底端中心（角色脚部位置）
- **平面**：几何中心

### 形状缩放
```csharp
// 立方体缩放：宽度(X), 厚度(Y), 高度(Z)
var cubeScale = new Vector3(width, depth, height);

// 胶囊体缩放：横截面(X), 横截面(Y), 高度(Z)
var capsuleScale = new Vector3(radius, radius, height);
```

## 📝 AI助手开发提示

1. **始终记住**：Z轴是高度，XY是地面平面
2. **跳跃动作**：使用Z轴速度
3. **重力物理**：作用于Z轴负方向
4. **碰撞检测**：地面接触检查Z坐标
5. **相机设置**：从适当的Z高度俯视XY平面

## 🔗 相关文档
- [形状系统原点位置指南](../MODEL_ORIGIN_GUIDE.md)
- [Actor系统文档](./systems/ActorSystem.md)
- [物理系统文档](./systems/PhysicsSystem.md)

---

> 💡 **关键提醒**：在进行任何3D相关开发时，请始终确认您使用的是Z轴作为高度轴。这将避免方向混淆和物理错误。
